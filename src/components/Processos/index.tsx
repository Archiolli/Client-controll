'use client'
import React from 'react'
import { Consultor, LoginResponse, Processo } from '@/@types/types'
import Container from '../Container'

const Processos = ({ consultores, user, processos }: { consultores?: Consultor[], user: number, processos: Processo[] }) => {
    return (
        <Container>
            

        </Container>
    )
}

export default Processos